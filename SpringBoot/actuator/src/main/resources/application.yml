management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    flyway:
      enabled: true
    health:
      roles:
      show-details: when_authorized
info:
  version: @project.version@